<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.englishword">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_ver"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_ver"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".header"></activity>
        <activity android:name=".loading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".word_test" />
        <activity
            android:name=".select"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".word_management" />
        <activity android:name=".papagohelp" />
        <activity android:name=".main_page" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>